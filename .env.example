# Host that the browser uses to reach federated MFE remotes.
# Leave as "localhost" for single-machine demo, set to this machine's LAN IP
# (e.g. 192.168.0.187) to expose the demo to other machines on the network.
# Restart `npm run dev` after changing.
PUBLIC_HOST=localhost
