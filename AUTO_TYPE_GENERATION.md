# 🔄 Автоматична генерация на типове при Dev старт

## ✅ Конфигурация

TypeScript типовете сега се генерират **автоматично** всеки път при стартиране на dev режим.

### Root package.json

```json
{
  "scripts": {
    "dev": "npm run generate:types && concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  }
}
```

## 🚀 Използване

### Стартирай Dev режим

```bash
npm run dev
```

Това автоматично:
1. ✅ Генерира `swagger.json` от TypeScript дефиницията
2. ✅ Генерира TypeScript типове в `frontend/src/api/generated/`
3. ✅ Стартира backend сървър на порт 3000
4. ✅ Стартира frontend dev сървър на порт 5173

### Резултат

```
✅ Swagger document generated at: .../swagger.json
✔ api file "data-contracts.ts" created
✔ api file "http-client.ts" created
✔ api file "Api.ts" created
[Backend] Server running on port 3000
[Frontend] Local: http://localhost:5173
```

## 📋 Workflow

### При промяна на API

1. **Редактирай Swagger спецификация**
   ```typescript
   // packages/backend/src/swagger.ts
   // Добави нови endpoints или промени съществуващи
   ```

2. **Рестартирай dev режим**
   ```bash
   # Спри текущия процес (Ctrl+C)
   npm run dev
   ```

3. **TypeScript типовете са обновени!**
   - Frontend автоматично вижда новите типове
   - TypeScript показва грешки ако API се е променило
   - IntelliSense показва новите полета

## 🎯 Предимства

✅ **Нулева ръчна работа** - Типовете винаги са актуални  
✅ **Невъзможна десинхронизация** - Backend и frontend винаги съвпадат  
✅ **Моментална обратна връзка** - Грешките се виждат веднага  
✅ **Чист workflow** - Една команда прави всичко  

## 🔧 Ръчна генерация

Ако искаш да генерираш типове без да стартираш серверите:

```bash
npm run generate:types
```

## 📝 Генерирани файлове

При всеки dev старт се пресъздават:

```
packages/frontend/src/api/generated/
├── data-contracts.ts    # TypeScript интерфейси
├── Api.ts              # API client клас
└── http-client.ts      # HTTP базов клас
```

**Внимание:** Не редактирай тези файлове - те се презаписват автоматично!

## 🔄 Пълен цикъл

```bash
# 1. Промени API
vim packages/backend/src/swagger.ts

# 2. Стартирай dev (типовете се генерират автоматично)
npm run dev

# 3. Frontend виждат новите типове веднага
# 4. TypeScript компилаторът показва къде да обновиш компонентите
```

## ⚡ Скорост

Генерирането отнема ~2 секунди и се случва **веднъж** при старт, не при всяка промяна на файл.

## 📚 Документация

- **[TYPE_GENERATION.md](./TYPE_GENERATION.md)** - Пълно ръководство
- **[SWAGGER_INTEGRATION.md](./SWAGGER_INTEGRATION.md)** - Интеграция
- **[README.md](./README.md)** - Главна документация
